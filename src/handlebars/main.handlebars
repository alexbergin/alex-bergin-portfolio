<!doctype html>
<html>
	<head>
		{{> partials/head }}
	</head>
	<body>
		{{> partials/background }}
		{{> partials/menu }}
		<main>
			{{> sections/index }}
			{{> sections/projects }}
		</main>
		{{> partials/navigation }}
	</body>
	<script src="scripts/main.js"></script>
</html>